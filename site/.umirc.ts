import { defineConfig } from 'dumi';

export default defineConfig({
  title: 'vs-split-view',
  mode: 'site',
  base: '/vs-splitview-react',
  publicPath: '/vs-splitview-react/',
  history: { type: 'hash' },
  // more config: https://d.umijs.org/config
});
